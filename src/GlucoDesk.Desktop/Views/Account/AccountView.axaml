<UserControl
    x:Class="GlucoDesk.Desktop.Views.Account.AccountView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Border Classes="page-shell">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">

      <Grid RowDefinitions="Auto,Auto"
            Margin="30"
            RowSpacing="26"
            MinWidth="860"
            MaxWidth="1680"
            HorizontalAlignment="Stretch">

        <Grid Grid.Row="0"
              ColumnDefinitions="*,Auto"
              ColumnSpacing="24">

          <StackPanel Spacing="8">
            <TextBlock Text="Account"
                       Classes="page-title" />

            <TextBlock Text="Connect GlucoDesk to your Dexcom Share account using secure local credential storage."
                       Classes="secondary"
                       FontSize="16"
                       TextWrapping="Wrap" />
          </StackPanel>

          <Border MinWidth="260" Grid.Column="1"
                  Classes="badge-accent"
                  VerticalAlignment="Top">

            <TextBlock Text="{Binding CredentialStorageStatusText}"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource GlucoDesk.Brush.AccentStrong}" />
          </Border>
        </Grid>

        <Border Grid.Row="1"
                Classes="surface-card"
                Padding="24"
                VerticalAlignment="Top">

          <Grid ColumnDefinitions="1.08*,0.92*"
                ColumnSpacing="34">

            <StackPanel Grid.Column="0"
                        Spacing="22">

              <StackPanel Spacing="4">
                <TextBlock Text="Dexcom Share account"
                           Classes="section-title" />

                <TextBlock Text="Save your Dexcom Share account locally so GlucoDesk can reconnect automatically when the app starts."
                           Classes="secondary"
                           FontSize="13"
                           TextWrapping="Wrap" />
              </StackPanel>

              <Grid ColumnDefinitions="*,*"
                    ColumnSpacing="18">

                <Border Grid.Column="0"
                        Padding="16"
                        CornerRadius="18"
                        BorderThickness="1"
                        BorderBrush="{StaticResource GlucoDesk.Brush.BorderSoft}"
                        Background="{StaticResource GlucoDesk.Brush.SurfaceSoft}">

                  <StackPanel Spacing="7">
                    <TextBlock Text="Credential storage"
                               Classes="metric-label" />

                    <TextBlock Text="{Binding CredentialStorageStatusText}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource GlucoDesk.Brush.AccentStrong}"
                               TextWrapping="Wrap" />

                    <TextBlock Text="{Binding CredentialStorageDescriptionText}"
                               Classes="muted"
                               FontSize="12"
                               TextWrapping="Wrap" />
                  </StackPanel>
                </Border>

                <Border MinWidth="260" Grid.Column="1"
                        Padding="16"
                        CornerRadius="18"
                        BorderThickness="1"
                        BorderBrush="{StaticResource GlucoDesk.Brush.BorderSoft}"
                        Background="{StaticResource GlucoDesk.Brush.SurfaceSoft}">

                  <StackPanel Spacing="7">
                    <TextBlock Text="Connection diagnostics"
                               Classes="metric-label" />

                    <TextBlock Text="{Binding ConnectionDiagnosticStatusText}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource GlucoDesk.Brush.AccentStrong}"
                               TextWrapping="Wrap" />

                    <TextBlock Text="{Binding ConnectionDiagnosticDescriptionText}"
                               Classes="muted"
                               FontSize="12"
                               TextWrapping="Wrap" />
                  </StackPanel>
                </Border>
              </Grid>

              <Border Height="1"
                      Background="{StaticResource GlucoDesk.Brush.BorderSoft}" />

              <Grid RowDefinitions="Auto,Auto,Auto"
                    RowSpacing="18">

                <StackPanel Grid.Row="0"
                            Spacing="8">
                  <TextBlock Text="Region"
                             Classes="metric-label" />

                  <ComboBox ItemsSource="{Binding RegionOptions}"
                            SelectedItem="{Binding SelectedRegion}"
                            HorizontalAlignment="Stretch" />
                </StackPanel>

                <StackPanel Grid.Row="1"
                            Spacing="8">
                  <TextBlock Text="Dexcom email"
                             Classes="metric-label" />

                  <TextBox Text="{Binding EmailText}"
                           PlaceholderText="name@example.com"
                           HorizontalAlignment="Stretch" />
                </StackPanel>

                <StackPanel Grid.Row="2"
                            Spacing="8">
                  <TextBlock Text="Dexcom password"
                             Classes="metric-label" />

                  <TextBox Text="{Binding PasswordText}"
                           PasswordChar="•"
                           PlaceholderText="Leave empty to keep the saved password"
                           HorizontalAlignment="Stretch" />

                  <TextBlock Text="{Binding PasswordHelpText}"
                             Classes="muted"
                             FontSize="12"
                             TextWrapping="Wrap" />
                </StackPanel>
              </Grid>

              <Border Height="1"
                      Background="{StaticResource GlucoDesk.Brush.BorderSoft}" />

              <Grid RowDefinitions="Auto,Auto,Auto"
                    RowSpacing="14">

                <WrapPanel Grid.Row="0"
                           Orientation="Horizontal">
                  <Button Content="Save account"
                          Command="{Binding SaveCommand}"
                          Margin="0,0,10,10" />

                  <Button Content="Test connection"
                          Command="{Binding TestConnectionCommand}"
                          Margin="0,0,10,10" />

                  <Button Content="Load saved account"
                          Command="{Binding LoadCommand}"
                          Classes="segment-button"
                          Margin="0,0,10,10" />

                  <Button Content="Clear account"
                          Command="{Binding ClearCredentialsCommand}"
                          Background="{StaticResource GlucoDesk.Brush.DangerPale}"
                          Foreground="{StaticResource GlucoDesk.Brush.Danger}"
                          BorderBrush="#FCA5A5"
                          Margin="0,0,10,10" />
                </WrapPanel>

                <TextBlock Grid.Row="1"
                           Text="{Binding StatusMessage}"
                           Classes="secondary"
                           TextWrapping="Wrap" />

                <Border Grid.Row="2"
                        IsVisible="{Binding HasError}"
                        Background="{StaticResource GlucoDesk.Brush.DangerSoft}"
                        BorderBrush="{StaticResource GlucoDesk.Brush.Danger}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="14">

                  <TextBlock Text="{Binding ErrorMessage}"
                             TextWrapping="Wrap"
                             FontWeight="SemiBold"
                             Foreground="{StaticResource GlucoDesk.Brush.Danger}" />
                </Border>
              </Grid>

            </StackPanel>

            <Border MinWidth="260" Grid.Column="1"
                    BorderThickness="1,0,0,0"
                    BorderBrush="{StaticResource GlucoDesk.Brush.BorderSoft}"
                    Padding="28,0,0,0">

              <StackPanel Spacing="20">

                <StackPanel Spacing="4">
                  <TextBlock Text="How it works"
                             Classes="section-title" />

                  <TextBlock Text="GlucoDesk uses Dexcom Share to read near real-time glucose data from your Dexcom account."
                             Classes="secondary"
                             FontSize="13"
                             TextWrapping="Wrap" />
                </StackPanel>

                <StackPanel Spacing="16">

                  <Grid ColumnDefinitions="Auto,*"
                        ColumnSpacing="12">
                    <Border Width="10"
                            Height="10"
                            CornerRadius="999"
                            Background="{StaticResource GlucoDesk.Brush.Accent}"
                            VerticalAlignment="Top"
                            Margin="0,5,0,0" />

                    <StackPanel Grid.Column="1"
                                Spacing="4">
                      <TextBlock Text="Automatic reconnect"
                                 FontWeight="SemiBold"
                                 Foreground="{StaticResource GlucoDesk.Brush.AccentStrong}" />

                      <TextBlock Text="After saving your account, GlucoDesk selects Dexcom Share as the active provider and can reconnect after app restart."
                                 TextWrapping="Wrap"
                                 Classes="secondary"
                                 FontSize="13" />
                    </StackPanel>
                  </Grid>

                  <Grid ColumnDefinitions="Auto,*"
                        ColumnSpacing="12">
                    <Border Width="10"
                            Height="10"
                            CornerRadius="999"
                            Background="{StaticResource GlucoDesk.Brush.Indigo}"
                            VerticalAlignment="Top"
                            Margin="0,5,0,0" />

                    <StackPanel Grid.Column="1"
                                Spacing="4">
                      <TextBlock Text="Connection test"
                                 FontWeight="SemiBold"
                                 Foreground="{StaticResource GlucoDesk.Brush.Indigo}" />

                      <TextBlock Text="Use Test connection after changing email, password or region. A successful test means Dexcom Share accepted the current credentials."
                                 TextWrapping="Wrap"
                                 Classes="secondary"
                                 FontSize="13" />
                    </StackPanel>
                  </Grid>

                  <Grid ColumnDefinitions="Auto,*"
                        ColumnSpacing="12">
                    <Border Width="10"
                            Height="10"
                            CornerRadius="999"
                            Background="{StaticResource GlucoDesk.Brush.Indigo}"
                            VerticalAlignment="Top"
                            Margin="0,5,0,0" />

                    <StackPanel Grid.Column="1"
                                Spacing="4">
                      <TextBlock Text="Privacy"
                                 FontWeight="SemiBold"
                                 Foreground="{StaticResource GlucoDesk.Brush.Indigo}" />

                      <TextBlock Text="Credentials stay on your computer. They are not committed to Git and are not stored in local JSON settings."
                                 TextWrapping="Wrap"
                                 Classes="secondary"
                                 FontSize="13" />
                    </StackPanel>
                  </Grid>

                  <Grid ColumnDefinitions="Auto,*"
                        ColumnSpacing="12">
                    <Border Width="10"
                            Height="10"
                            CornerRadius="999"
                            Background="{StaticResource GlucoDesk.Brush.Warning}"
                            VerticalAlignment="Top"
                            Margin="0,5,0,0" />

                    <StackPanel Grid.Column="1"
                                Spacing="4">
                      <TextBlock Text="Safety notice"
                                 FontWeight="SemiBold"
                                 Foreground="{StaticResource GlucoDesk.Brush.Warning}" />

                      <TextBlock Text="GlucoDesk is not a medical device. Always use official Dexcom and Omnipod apps for treatment decisions."
                                 TextWrapping="Wrap"
                                 Classes="secondary"
                                 FontSize="13" />
                    </StackPanel>
                  </Grid>

                </StackPanel>
              </StackPanel>
            </Border>

          </Grid>
        </Border>
      </Grid>
    </ScrollViewer>
  </Border>
</UserControl>
